const namespaced = true

const state = {
  notes: [],
  opened: false
}

const mutations = {}

const actions = {}

const getters = {}

export default { namespaced, state, mutations, actions, getters }
